# Frontend environment variables.
# For local dev, leave empty (Vite proxies /api to http://localhost:8000).
# For production (Vercel), set this to your backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=